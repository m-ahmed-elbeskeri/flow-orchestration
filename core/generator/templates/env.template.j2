# {{ spec.name }} Environment Variables
# Copy this file to .env and fill in your values

{% for secret in spec.all_secrets -%}
{{ secret }}=your_{{ secret.lower() }}_here
{% endfor %}

# Optional settings
LOG_LEVEL=INFO
