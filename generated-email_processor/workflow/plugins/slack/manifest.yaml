name: slack
version: 1.0.0
description: Slack integration for messaging and workspace operations
author: Workflow Orchestrator Team

states:
  send_message:
    description: "send_message state"
  read_messages:
    description: "read_messages state"
  upload_file:
    description: "upload_file state"
  create_channel:
    description: "create_channel state"

resources:
  cpu_units: 0.1
  memory_mb: 100
  network_weight: 1.0

dependencies:
  - slack-sdk
  - aiofiles
